server:
  port: 8080

spring:
  application:
    name: summarizer

  ai:
    openai:
      apikey: ${OPENROUTER_API_KEY}
      base-url: https://openrouter.ai/api
      chat:
        completions-path: /v1/chat/completions
        model: ${CHAT_MODEL}
        options:
          max-tokens: 2000
