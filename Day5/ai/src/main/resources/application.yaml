server:
  port: 8080

spring:
  application:
    name: ai

  ai:
    openai:
      api-key: ${OPENROUTER_API_KEY}
      base-url: https://openrouter.ai/api/v1
      chat:
        model: ${CHAT_MODEL}
        options:
          max-tokens: 2000

weather:
  api:
    key: ${WEATHER_API_KEY}